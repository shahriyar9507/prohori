# Base URL of the PRAHARI backend API.
# Local dev leaves this empty and uses the Vite proxy (see vite.config.js).
# For a deployed frontend, set the backend's public URL, e.g.:
# VITE_API_BASE_URL=https://prahari-backend.onrender.com
VITE_API_BASE_URL=
